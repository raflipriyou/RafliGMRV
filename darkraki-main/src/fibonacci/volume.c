#include <stdio.h>

int main() {
    int radius = 6;
    float pi = 3.14;
    int volume = (int)((4.0/3.0) * pi * radius * radius * radius);
    //printf("The volume of the sphere is %d\n", volume);
    return volume;
}